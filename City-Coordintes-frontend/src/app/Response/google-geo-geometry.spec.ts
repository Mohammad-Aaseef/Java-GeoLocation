import { GoogleGeoGeometry } from './google-geo-geometry';

describe('GoogleGeoGeometry', () => {
  it('should create an instance', () => {
    expect(new GoogleGeoGeometry()).toBeTruthy();
  });
});
